@extends('layouts.app')

@section('content')
    <div class="container">
        <div class="row">
            @include('admin.sidebar')

            <div class="col-md-9">
                <div class="panel panel-default">
                    <div class="panel-heading">SensorUnit {{ $sensorunit->serial }}</div>
                    <div class="panel-body">
                        <a href="{{ url('/sensor-units') }}" title="Back"><button class="btn btn-warning btn-xs"><i class="fa fa-arrow-left" aria-hidden="true"></i> Back</button></a>
                        <a href="{{ url('/sensor-units/' . $sensorunit->serial . '/edit') }}" title="Edit SensorUnit"><button class="btn btn-primary btn-xs"><i class="fa fa-pencil-square-o" aria-hidden="true"></i> Edit</button></a>
                        {!! Form::open([
                            'method'=>'DELETE',
                            'url' => ['sensorunits', $sensorunit->serial],
                            'style' => 'display:inline'
                        ]) !!}
                            {!! Form::button('<i class="fa fa-trash-o" aria-hidden="true"></i> Delete', array(
                                    'type' => 'submit',
                                    'class' => 'btn btn-danger btn-xs',
                                    'title' => 'Delete SensorUnit',
                                    'onclick'=>'return confirm("Confirm delete?")'
                            ))!!}
                        {!! Form::close() !!}
                        <br/>
                        <br/>

                        <div class="table-responsive">
                            <table class="table table-borderless">
                                <tbody>
                                <tr>
                                    <th> Serial</th>
                                    <td> {{ $sensorunit->serial }} </td>
                                </tr>
                                <tr>
                                    <th> Description</th>
                                    <td> {{ $sensorunit->description }} </td>
                                </tr>
                                <tr>
                                    <th> Last Seen</th>
                                    <td> {{ $sensorunit->last_seen }} </td>
                                </tr>
                                </tbody>
                            </table>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>
@endsection
